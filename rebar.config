
% NOTE: this is not really necessary as we still had to add "-pa ebin" the
% ./rebar itself in order to substitute the native Erlang parser with our custom
% one (this is needed only for the test).

{erl_first_files, ["src/erl_parse.erl"]}.
{eunit_first_files, ["src/erl_parse.erl"]}.

